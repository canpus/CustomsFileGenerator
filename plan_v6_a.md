# 报关资料自动生成系统 — 实施计划 v6.0-a（Agent 版 · 暂缓 DOCX）

> **目标读者**：AI Agent（每次一个对话窗口，按阶段执行）
> **版本**：6.0.1-a | **日期**：2026-05-12
> **技术栈**：tkinter + ttkbootstrap + openpyxl + python-docx + msgspec + sqlite3
> **体积红线**：< 50MB（目标 38-42MB）
>
> **与本家 plan_v6.md 的关系**：
> - 本文件为 `plan_v6.md` 的**先行可执行分支**
> - 暂时排除阶段 6（DOCX 报关单生成器），待以下前置条件满足后，再迁移回 `plan_v6.md` 完成剩余工作
> - 凡标注 `[待迁移]` 处，均需在合并回 `plan_v6.md` 时补全

---

## ⚠️ AGENT 使用说明（必读）

1. 本计划共 **10 个阶段**（阶段 6 暂缓），每个阶段在一个独立的对话窗口中完成
2. 每阶段开头会标注 **📥 你需要在对话开始时提供给 Agent 的文件清单**
3. 每阶段结尾会标注 **✅ 里程碑验证方法** — Agent 必须产出能通过该验证的代码
4. **跨阶段依赖**：后续阶段依赖前序阶段产出的文件/数据模型，不可跳过
5. 若某阶段 Agent 产出不通过里程碑验证，在原对话窗口中继续修正，不要开启新窗口

---

## ⛔ 阶段 6 暂缓声明

> **状态**：🔴 暂缓（`TODO:待确认`）
> **原因**：DOCX 报关单模板存在三重不确定性：
> 1. 模板来源不明 — 不知道是海关标准格式还是公司自制
> 2. 格式约束不明 — 不知道单元格可否拆分、行数可否增加
> 3. 商品明细行的对齐机制不明 — 合并行 + 空格对齐 vs 独立单元格

### 恢复条件（全部满足后方可激活阶段 6）

| 序号 | 条件 | 确认方式 |
|------|------|---------|
| C1 | 货代/海关确认：单元格内文字内容可修改 | 电话/邮件 |
| C2 | 货代/海关确认：商品明细行数可增加 | 电话/邮件 |
| C3 | 货代/海关确认：表格结构（合并/拆分）是否允许 | 电话/邮件 |
| C4 | 对齐技术方案确定（`{{占位符}}` + 何种对齐策略） | 内部技术讨论 |
| C5 | 验证：生成一份报关单交货代验证格式合规 | 实操验证 |

### 迁移回 plan_v6.md 的步骤

```
1. 将本文件中的 [待迁移] 标记处全部替换为完整实现
2. 阶段 6 按确认后的技术方案重写
3. 阶段 7 协调器补充第 4 个生成器（CustomsGenerator）
4. 阶段 9 GUI 生成页恢复 4 文件输出
5. 阶段 🔟 build.spec 确认 python-docx 已包含
6. 全链路测试（4 文件生成 + 模板断言 + 格式验证）
```

---

## 阶段 0️⃣：项目骨架与环境自检

**📥 你需要提供给 Agent 的文件**：
- 根 `Claude.md`（本文件）
- `Claude.python.md`（Python 规范）
- `docs/pyproject.toml`（Ruff/mypy/pytest 配置）
- 四个真实模板文件（放入 `templates/` 目录）：
  - `template_customs.docx`（报关单模板 — **暂不使用，但保留完整性**）
  - `template_packing.xlsx`（装箱单模板）
  - `template_invoice.xlsx`（形式发票模板）
  - `template_contract.xlsx`（形式合同模板）

**Agent 任务**：

| 子任务 | 具体内容 |
|--------|---------|
| 0.1 | 创建完整项目目录结构（见下方） |
| 0.2 | 编写 `requirements.txt`（锁定精确版本号） |
| 0.3 | 编写 `main.py` 入口：环境自检（Python 版本 ≥ 3.10，模板文件存在性，依赖安装检查） |
| 0.4 | 编写 `config/settings.py`：从 `.env` + `settings.json` 读取配置 |
| 0.5 | 编写 `config/constants.py`：所有默认值、枚举值、模板路径常量 |
| 0.6 | 创建 `.env.example` 模板文件 |

**目录结构**：

```
CustomsFileGenerator/
├── main.py
├── requirements.txt
├── .env.example
├── .gitignore
├── config/
│   ├── __init__.py
│   ├── settings.py
│   ├── constants.py
│   └── settings.json
├── src/
│   ├── __init__.py
│   ├── models/
│   │   └── __init__.py
│   ├── db/
│   │   └── __init__.py
│   ├── generators/
│   │   └── __init__.py
│   ├── importer/
│   │   └── __init__.py
│   └── gui/
│       ├── __init__.py
│       └── pages/
│           └── __init__.py
├── templates/              ← 你放入 4 个模板文件
│   ├── template_customs.docx
│   ├── template_packing.xlsx
│   ├── template_invoice.xlsx
│   └── template_contract.xlsx
├── output/                 ← 生成的文件输出目录
├── docs/
│   └── schemas/
└── tests/
    └── __init__.py
```

**✅ 里程碑 0 验证方法**：
```bash
# 在项目根目录执行：
python main.py

# 预期输出（模板文件都存在时）：
# ✅ Python 版本：3.12.0 — 通过
# ✅ 依赖检查：全部已安装
# ✅ 模板文件：4/4 存在
# 🚀 环境自检通过，程序就绪

# 如果缺少某个模板文件，预期输出：
# ❌ 模板文件缺失：templates/template_customs.docx
# 💡 请将报关单模板文件放入 templates/ 目录
# 🔧 模板文件名必须为：template_customs.docx
```

---

## 阶段 1️⃣：统一数据模型（msgspec 结构体）

**📥 你需要提供给 Agent 的文件**：
- 阶段 0 产出的所有文件
- 你的真实订单数据示例（至少 1 个完整订单的 Excel 或手写数据）
- `RealOrderData.md`（真实订单数据参考文档）

**Agent 任务**：

| 子任务 | 具体内容 |
|--------|---------|
| 1.1 | 编写 `src/models/order_data.py`：定义 `OrderData`、`OrderMeta`、`Customer`、`Origin`、`Pallet`、`Carton`、`Product`、`Totals` 等 msgspec.Struct 结构体，**严格遵循本计划末尾的完整 JSON Schema** |
| 1.2 | 编写 `src/models/validators.py`：实现 `validate_order_consistency()` 函数，检查以下业务规则： |
| | - 总毛重 ≥ 总净重 |
| | - 每个纸箱毛重 ≥ 箱内所有商品净重之和 |
| | - 托盘总体积 = 长×宽×高（允许 ±0.001 误差） |
| | - 汇总数据与明细加总一致（交叉校验） |
| | - 所有必填字段不为空 |
| 1.3 | 编写 `src/models/error_mapper.py`：将 msgspec 的 `ValidationError` 映射为中文修复建议（三要素：❌发生了什么 / 💡原因 / 🔧解决方法） |

**数据流向**：
```
JSON 字符串 → msgspec.decode() → OrderData 对象 → validators.validate_order_consistency() → (通过/警告/拒绝)
```

**✅ 里程碑 1 验证方法**：
```bash
python -m pytest tests/test_models.py -v

# 测试文件必须覆盖：
# 1. 合法 JSON → 成功反序列化
# 2. 缺少必填字段 → error_mapper 返回中文错误
# 3. 类型错误（字符串填数字字段）→ error_mapper 返回中文错误
# 4. 枚举值非法（运输方式填"高铁"）→ error_mapper 返回中文错误
# 5. 总毛重 < 总净重 → validate_order_consistency 返回警告
# 6. 纸箱毛重 < 商品净重之和 → validate_order_consistency 返回警告
# 7. 空订单（pallets 为空数组）→ 拒绝
# 8. 超大订单（100+ 托盘）→ 通过但打印性能提示

# 预期：8 个测试全部 PASSED
```

---

## 阶段 2️⃣：SQLite 数据库层

（内容与 plan_v6.md 完全相同，此处省略重复内容以节省阅读量）
> **Agent 提醒**：执行时请直接参考 `plan_v6.md` 阶段 2 的完整内容。

---

## 阶段 3️⃣：XLSX 工具库（openpyxl 安全操作）

（内容与 plan_v6.md 完全相同）
> **Agent 提醒**：执行时请直接参考 `plan_v6.md` 阶段 3 的完整内容。

---

## 阶段 4️⃣：装箱单生成器（Packing Generator）

（内容与 plan_v6.md 完全相同）
> **Agent 提醒**：执行时请直接参考 `plan_v6.md` 阶段 4 的完整内容。

---

## 阶段 5️⃣：发票 + 合同生成器（Invoice & Contract Generator）

（内容与 plan_v6.md 完全相同，含 `base_generator.py` 抽象基类）
> **Agent 提醒**：执行时请直接参考 `plan_v6.md` 阶段 5 的完整内容。

---

## 阶段 6️⃣：报关单生成器 — 🔴 暂缓 ⛔

> **状态**：`TODO:待确认` — 本阶段**不在本计划中执行**
> **执行时机**：满足下方恢复条件后，在 `plan_v6.md` 中开发

### 前置确认清单

在编写任何报关单生成代码之前，**必须**完成以下确认：

| # | 确认项 | 确认人 | 状态 |
|---|--------|--------|------|
| 1 | 单元格内文字内容可以修改？ | 货代/海关 | ⬜ |
| 2 | 商品明细行数可以增加？ | 货代/海关 | ⬜ |
| 3 | 表格单元格可以拆分/合并？ | 货代/海关 | ⬜ |
| 4 | 对齐技术方案确定（`{{占位符}}` + 策略） | 技术负责人 | ⬜ |
| 5 | 生成样本交货代验证格式合规 | 货代 | ⬜ |

### 已知技术约束（讨论中确定）

| 约束 | 说明 |
|------|------|
| 🔴 表格结构不可动 | 在确认模板来源之前，禁止拆分/合并单元格、禁止增减列、禁止修改合并区域 |
| 🟡 仅允许文字替换 | `{{占位符}}` → 真实数据的 Run 级替换是当前最安全的操作 |
| 🟡 对齐依赖实际结构 | 合并行+空格对齐 vs 独立单元格，实现方案完全不同 |
| 🟢 沙箱操作 | 所有操作在 `tempfile.gettempdir()` 副本上进行，不碰原模板 |

### 技术方案预案（待确认后选定）

| 方案 | 前提条件 | 对齐精度 | 代码复杂度 |
|------|---------|---------|-----------|
| A: 独立单元格 + `{{占位符}}` | C3=允许拆分单元格 | ⭐⭐⭐⭐⭐ | 低（复用 XLSX 逻辑） |
| B: 合并行 + 字符串拼接 | C3=不允许 | ⭐⭐⭐ | 中 |
| C: 仅替换表头 + 人工补明细 | C1=允许文字修改 | ⭐⭐ | 极低 |

### 恢复执行参照

当上述 5 项前置条件全部通过后，按 `plan_v6.md` 阶段 6 的完整内容执行，并将本阶段替换为具体代码。

---

## 阶段 7️⃣：一键生成 + 模板无损断言（3 文件模式）`[待迁移]`

**📥 你需要提供给 Agent 的文件**：
- 阶段 0-5 产出的所有文件

**Agent 任务**：

| 子任务 | 具体内容 |
|--------|---------|
| 7.1 | 编写 `src/generators/orchestrator.py`：一键生成协调器（3 文件模式） |
| | - 接收 `OrderData` + 输出目录 → 依次调用 **3 个**生成器（Packing + Invoice + Contract）|
| | - 实时进度回调（哪个文件正在生成，百分比） |
| | - 错误隔离：某个生成器失败不影响其他生成器 |
| | - **[待迁移]**：架构预留第 4 个生成器插槽（`CustomsGenerator`），当前返回 `(skipped, "报关单暂未就绪")` |
| 7.2 | 编写 `src/generators/template_assertion.py`：模板无损断言引擎 |
| | - xlsx 断言：检查关键单元格的字体、边框、合并范围、公式是否与模板一致 |
| | - **[待迁移]** docx 断言：检查表格数量、表头行数、关键占位符存在性（预留接口，当前跳过） |
| | - 断言分级：error（阻断）/ warning（警告）/ info（日志） |
| | - 可配置规则：从 `config/assertion_rules.json` 读取 |
| 7.3 | 编写 `config/assertion_rules.json`：定义各模板的断言规则（packing/invoice/contract 三模板） |
| | - **[待迁移]**：customs 断言规则预留空节，标注 `TODO:待确认` |
| 7.4 | 编写 `src/generators/template_guard.py`：模板保护机制 |
| | - 生成前校验模板存在性和完整性 |
| | - 沙箱操作（复制到临时目录） |
| | - 出厂默认模板自动恢复（当模板损坏时从 `src/assets/backup_templates/` 恢复） |
| 7.5 | 创建 `src/assets/backup_templates/` 目录，放入 4 个模板的备份副本 |

**✅ 里程碑 7 验证方法**：
```bash
python -m pytest tests/test_orchestrator.py tests/test_template_assertion.py -v

# 测试文件必须覆盖：
# 1. 一键生成：给定一个合法 OrderData → 输出 3 个文件 → 全部可打开
#    [待迁移] 阶段 6 完成后改为 4 个文件
# 2. 一键生成进度回调：验证回调被调用了 3 次（每个文件一次）
#    [待迁移] 阶段 6 完成后改为 4 次
# 3. 错误隔离：模拟 packing 生成失败 → invoice/contract 仍然生成
# 4. xlsx 断言通过：生成的文件字体/边框/合并与模板一致
# 5. xlsx 断言失败：模拟字体被篡改 → 断言报告显示 error
# 6. [待迁移] docx 断言通过：表格数量、表头行数、占位符全部正确
# 7. [待迁移] docx 断言失败：模拟表格缺失 → 断言报告显示 error
# 8. 模板损坏恢复：删除 packing 模板 → 自动从 backup_templates 恢复
# 9. 断言分级：字体大小偏差 0.5pt → 报 warning（不阻断），合并单元格丢失 → 报 error

# 当前预期：5/7 项 PASSED（#1/#2 按 3 文件模式，#6/#7 暂缓）
# 阶段 6 完成后预期：9/9 PASSED
```

---

## 阶段 8️⃣：数据导入器 + 诊断包

（内容与 plan_v6.md 完全相同，不依赖阶段 6）
> **Agent 提醒**：执行时请直接参考 `plan_v6.md` 阶段 8 的完整内容。

---

## 阶段 9️⃣：GUI 界面（ttkbootstrap · 3 文件模式）`[待迁移]`

**📥 你需要提供给 Agent 的文件**：
- 阶段 0-8 产出的所有文件

**Agent 任务**：

| 子任务 | 具体内容 |
|--------|---------|
| 9.1 | 编写 `src/gui/app.py`：主应用窗口 |
| | - 基于 ttkbootstrap（Flatly 主题） |
| | - 入口激活 DPI 感知（`ctypes.windll.shcore.SetProcessDpiAwareness(1)`） |
| | - 字体回退池（Microsoft YaHei → Segoe UI → TkDefaultFont） |
| | - 左侧导航栏（新建单据 / 历史模板 / 客户管理 / 产品库） |
| | - 右侧工作区（根据导航切换页面） |
| 9.2 | 编写 `src/gui/pages/order_info_page.py`：订单信息录入页 |
| 9.3 | 编写 `src/gui/pages/tree_editor_page.py`：托盘-纸箱-商品树状编辑器 |
| 9.4 | 编写 `src/gui/pages/import_page.py`：表格导入页 |
| 9.5 | 编写 `src/gui/pages/template_page.py`：模板管理页 |
| 9.6 | 编写 `src/gui/pages/generate_page.py`：生成与预览页（3 文件模式） |
| | - 一键生成按钮 → 进度条 → 完成提示 |
| | - 生成清单显示 **3 项**（装箱单 ✅ / 发票 ✅ / 合同 ✅ / 报关单 ⏳ 暂未就绪） |
| | - 诊断包导出按钮（仅出错时显示） |
| | - 打开输出文件夹按钮 |
| | - **[待迁移]**：阶段 6 完成后，报关单行改为可交互生成 |

**✅ 里程碑 9 验证方法**：
```bash
# 启动 GUI：
python main.py --gui

# 人工验证清单：
# □ 窗口正常打开，DPI 缩放下无模糊
# □ 左侧导航 4 个按钮可切换页面
# □ 订单信息页：所有输入框可录入，下拉框（运输方式等）可选值
# □ 树状编辑器：可新增托盘 → 新增纸箱 → 新增商品，三级结构正确
# □ 右键菜单：克隆托盘（含其下所有纸箱和商品）功能正常
# □ 表格导入：选择一个 Excel 文件 → 预览数据 → 确认后填充到树状编辑器
# □ 模板保存/加载：保存当前订单 → 关闭程序 → 重新打开 → 加载模板 → 数据一致
# □ 一键生成：点击生成按钮 → 进度条走完 → 提示成功 → output/ 目录有 3 个文件
#   [待迁移] 阶段 6 完成后应为 4 个文件
# □ 完整性校验：总毛重 < 总净重时 → 红色警告，禁止生成
# □ 诊断包导出：故意制造错误 → 点击导出 → 生成 zip 文件
```

---

## 阶段 🔟：打包 + CI 验证 + 签名 SOP `[待迁移]`

> **⚠️ 注意**：本阶段在阶段 6 完成之前，打包产物将**不含 DOCX 报关单生成功能**。
> `build.spec` 中 `python-docx` 依赖保留，但 CustomGenerator 模块暂不参与打包。

**Agent 任务**（与 plan_v6.md 相同，增加以下注意事项）：

| 编号 | 注意事项 |
|------|---------|
| 🔟-A | `build.spec` 中 `datas` 包含 `templates/template_customs.docx`（即使暂不使用） |
| 🔟-B | `verify_build.py` 的模板完整性检查目标为 4 个文件（含 customs） |
| 🔟-C | **[待迁移]**：阶段 6 完成后，重新打包验证 4 文件生成功能 |

> 完整任务清单请直接参考 `plan_v6.md` 阶段 🔟。

---

## 📋 阶段依赖关系图（调整后）

```
阶段 0（骨架+环境自检）
  ↓
阶段 1（数据模型）
  ↓
阶段 2（数据库）
  ↓
阶段 3（XLSX 工具库）           ← 关键！后续所有 xlsx 生成器依赖此阶段
  ↓
阶段 4（装箱单生成器）
阶段 5（发票+合同生成器）
阶段 6（⛔ 暂缓 — DOCX 报关单）  ← 不在本计划中执行
  ↓
阶段 7（一键生成+断言 · 3文件）  ← 依赖 4+5
  ↓
阶段 8（导入器+诊断包）         ← 可与阶段 9 并行
阶段 9（GUI 界面 · 3文件）      ← 可与阶段 8 并行
  ↓
阶段 🔟（打包+CI+签名）         ← 依赖所有前序阶段（含预留 customs 模板）
```

### 恢复阶段 6 后的依赖图（同 plan_v6.md）

```
阶段 3（XLSX 工具库）
  ↓
阶段 4（装箱单生成器）
阶段 5（发票+合同生成器）
阶段 6（报关单生成器 — docx）   ← 恢复后激活
  ↓
阶段 7（一键生成+断言 · 4文件）  ← 恢复为 4 文件模式
  ↓
阶段 8（导入器+诊断包）
阶段 9（GUI 界面 · 4文件）
  ↓
阶段 🔟（打包+CI+签名）
```

---

## 🔗 附录 A：完整 JSON Schema

> 与 `plan_v6.md` 附录 A 完全相同。
> **Agent 提醒**：执行阶段 1 时请直接参考 `plan_v6.md` 附录 A。

---

## 🔗 附录 B：XLSX 模板字段映射表

> 与 `plan_v6.md` 附录 B 完全相同（装箱单、形式发票、形式合同三个 xlsx 模板）。
> **Agent 提醒**：执行阶段 3-5 时请直接参考 `plan_v6.md` 附录 B。

---

## 🔗 附录 C：阶段 6 恢复指引 [新增]

> 本附录仅供用户阅读，AI Agent 在执行本计划 (`plan_v6_a.md`) 时**不应执行阶段 6**。

### C.1 状态转换流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     plan_v6_a.md（当前）                         │
│                                                                 │
│  阶段 0 → 1 → 2 → 3 → 4 → 5 → 7(3文件) → 8 → 9(3文件) → 🔟    │
│                              ⛔ 阶段 6 暂缓                     │
│                                                                 │
│  产出：                                                         │
│    ✅ 数据模型 + DB + XLSX 工具库                               │
│    ✅ 装箱单生成器                                              │
│    ✅ 发票 + 合同生成器                                         │
│    ✅ 3 文件协调器 + 模板断言（xlsx 部分）                       │
│    ✅ Excel 导入器 + 诊断包 + 脱敏                              │
│    ✅ GUI（3 文件模式）                                         │
│    ✅ 打包脚本（不含 DOCX 生成功能）                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ 前置条件全部满足后
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     plan_v6.md（迁移后）                         │
│                                                                 │
│  复用 0-5 + 8 全部代码                                          │
│  新增 阶段 6（DOCX 报关单生成器）                                │
│  修改 阶段 7 的 4 个 [待迁移] 标记                               │
│  修改 阶段 9 的 generate_page.py                                │
│  修改 阶段 🔟 验证脚本                                          │
│                                                                 │
│  最终产出：4 文件完整生成系统                                    │
└─────────────────────────────────────────────────────────────────┘
```

### C.2 各阶段迁移修改清单

| 阶段 | 文件 | 需要修改的内容 |
|------|------|---------------|
| 6 | `src/generators/customs_generator.py`（新增） | DOCX 报关单生成器 |
| 6 | `src/generators/template_anchor_scanner.py`（修改） | 新增 `scan_docx_placeholders()` |
| 7 | `src/generators/orchestrator.py`（修改） | 取消占位桩，接入 `CustomsGenerator`；预期输出 4 文件 |
| 7 | `src/generators/template_assertion.py`（修改） | 实现 docx 断言逻辑 |
| 7 | `config/assertion_rules.json`（修改） | 补充 customs 断言规则 |
| 7 | `tests/test_orchestrator.py`（修改） | #1 改 4 文件，#2 改 4 次 |
| 7 | `tests/test_template_assertion.py`（新增） | #6/#7 docx 断言测试 |
| 9 | `src/gui/pages/generate_page.py`（修改） | 报关单行改为可交互 |
| 🔟 | `verify_build.py`（修改） | 验证 4 文件生成 + DOCX 功能 |

### C.3 建议的迁移窗口（新对话）

```
对话窗口 N：阶段 6 完成后
  ├── 输入：本 plan_v6_a.md + 阶段 0-5 全部产物 + 阶段 6 产物
  ├── 任务：按 C.2 清单逐一修改
  ├── 验证：pytest 全量回归 + 人工 4 文件验证
  └── 输出：功能完整的 CustomsFileGenerator
```

---

> **修订历史**
> - 2026-05-12（v6.0.1-a）：基于 plan_v6.md v6.0.0 建立先行分支。阶段 6（DOCX 报关单）暂缓，阶段 7/9/🔟 调整为 3 文件模式，新增附录 C 恢复指引。
> - 2026-05-10（v6.0.0·plan_v6.md）：初始版本，含全部 10 个阶段。

---

> **Agent 执行提醒**：
> 1. 本文件为可执行计划，阶段 0-5 + 7-🔟 均应按要求输出代码
> 2. 阶段 6 不可执行 — 如被要求执行，请提醒用户先完成附录 C.1 的前置条件
> 3. 凡标注 `[待迁移]` 的代码段，需预留扩展点（接口、回调参数），避免阶段 6 完成后大规模重构
> 4. 所有生成的代码必须符合根 `Claude.md` 的硬约束卡要求
